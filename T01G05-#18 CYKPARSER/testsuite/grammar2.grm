START := A B;
START := B S1|A;
START := B Va;
S1 := A B;
A := B A;
A := a;
B := Va S1;
B := S1 Va;
B := S1 S1;
B := b;
Va := a;
END

# grammar 2
In this example, a production (START:=A in line 2), violates one of the Chomsky normal form rules.
It was created with the intent to test the detection and conversion of unitary productions
into the CNF form.
